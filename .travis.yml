language: python

python:
  - 3.6

addons:
  apt:
    packages:
      - docker-ce

service:
  - docker

cache:
  directories:
    - /home/travis/docker/

before_install:
  - make build

after_success:
  - make push
